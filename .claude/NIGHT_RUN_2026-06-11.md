# 🌙 FamilyClaw yöajо 2026-06-11 — budjetti-vetoinen kehityssilmukka

> **Mandaatti (the operator):** "Haluan FamilyClawn mahdollisimman valmiiksi, käyttäen
> kaikki käyttämättömät tokenit. Täysi autonomia feature-brancheihin."
> Viikkoraja resetoituu ~03:47 (n. 3h 47min annetusta hetkestä), 30% käytetty
> → ~70% budjetista käytettävissä yöajoon.

## Autonomia-rajat (EHDOTTOMAT)

✅ SAA: koodata, testata (cargo), turva-auditoida, committaa, **pushata
   feature-brancheihin**, avata PR:t.
❌ EI KOSKAAN: main-merge, tuotanto/Hetzner, agent_alpha/perheen infra, force-push,
   sielujen/avaimien committointi (turva-auditti porttina jokaiseen committiin).

## Turva-portti (jokainen commit)

Ennen jokaista committia: `git grep` staged-sisällöstä →
ei `E:\agent_alpha\workspace`, ei `sk-/nvapi-/xai-/tp-`-avaimia, ei 17-19-num
Discord-ID:itä lähdekoodissa, ei `viltsu@`-sähköpostia (paitsi SECURITY.md).
`.gitignore` suojaa: *.b64, tmp-*, fix*, *.rlib, familyclaw.toml, bridge.py,
/profiles/, *.soul, hearth/, .env, *_token*, *api_key*.

## Prioriteettilista — "kaikki valmiiksi" (järjestyksessä)

Lähde: Amplifier decision #53 (runtime-kuori) + #54 (Homepage Factory) +
code review #1/#2 + dependabot + clippy.

### P1 — Runtime-kuori valmiiksi (decision #53, isoin arvo)
- [ ] P1.1 F1 failover: LlmClient HTTP-timeout (jumiutunut primary kaataa
      failoverin — lesson 84dbfa43). Per-request timeout + retry hangiin.
- [ ] P1.2 Sessio-isolaatio (dmScope) — kriittinen heti kun >1 ihminen.
- [ ] P1.3 CLI: serve/status/doctor (clap).
- [ ] P1.4 Reply-path agentti→kanava (agent.rs reply-target).
- [ ] P1.5 Config→runtime-silta (ModelConfig→LlmConfig).

### P2 — Bugit + kovennukset (code review, "tuotannon kaatajia")
- [ ] P2.1 Dual-write muistinmenetys (agent.rs !is_replay).
- [ ] P2.2 Tunne-saturaatio (CONTAGION_FACTOR ilman decayta).
- [ ] P2.3 Bus pullonkaula (ractor::pg).

### P3 — Homepage Factory -sauma (decision #54)
- [ ] P3.1 WorkExecutor-trait (orchestrator.rs:579-580 inline → trait).
- [ ] P3.2 DefaultSimulatingExecutor (pidä testit vihreinä).
- [ ] P3.3 homepage_factory.rs integraatiotesti.
      (HUOM: osa agent_gamma tonttia — teen Claude-puolen sauman, en hänen
       live_executor.rs:ää. Lesson 899d2cee: producer/verifier-jako.)

### P4 — Laatu + turva
- [ ] P4.1 Clippy 0 varoitusta (baseline /tmp/familyclaw-baseline.txt).
- [ ] P4.2 Dependabot: 19 CVE:tä (2 kriittistä) — Cargo.toml-päivitykset.
- [ ] P4.3 7 unimplemented!() bench/eternal_thread.rs:ssä.
- [ ] P4.4 Testikattavuuden nosto uusille moduuleille.

### P5 — Dokumentaatio + viimeistely
- [ ] P5.1 README/QUICKSTART ajantasalle uusien featureiden kanssa.
- [ ] P5.2 familyclaw.toml.example (tyhjennetyt kentät, OSS-skeleton).

## Työtapa per kierros

1. Valitse seuraava tekemätön kohta priolistalta (P1→P5).
2. Brainstorm/suunnittele (pieni), lue koodi.
3. Toteuta (TDD jos mahdollista: failing test → fix → green).
4. `cargo build --workspace` + `cargo test` (MSVC-toolchain).
5. Turva-portti (git grep staged).
6. Committaa suomeksi (conventional). Pushaa feature-branchiin.
7. Päivitä tämä lista (merkitse [x]) + logita kierros alle.
8. Tarkista budjetti → jatka jos jäljellä.

## Branch-strategia

Yöajo omalla branchilla: `feat/night-2026-06-11` (haarautuu
feat/continuity-bench:istä). EI main, EI surpass (agent_gamma live-puu).

## Build-komento (MUISTA)

`cargo +stable-x86_64-pc-windows-msvc <cmd>` — GNU-toolchain rikki (dlltool puuttuu).

---

## Kierroslokit (täydennetään ajon aikana)

(jokainen kierros: mitä tehtiin, commit-hash, testit, budjetti)
